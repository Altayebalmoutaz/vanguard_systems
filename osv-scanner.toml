# Supply-chain ignore list for osv-scanner (CI supply-chain job).
# Prefer dependency bumps; only ignore when no compatible fix exists upstream.

[[IgnoredVulns]]
id = "GHSA-537c-gmf6-5ccf"
reason = "cryptography>=48.0.1 fixes this, but presidio-anonymizer==2.2.363 requires cryptography<47. Revisit when Presidio publishes a compatible release. owner: platform, reviewed: 2026-07-13"
